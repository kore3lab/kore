apiVersion: install.kore3lab.io/v1alpha1
kind: KoreOperator
metadata:
  name: dashboard
  namespace: kore-system
spec:
  components:
    base:
      enabled: false
    dashboard:
      enabled: true
  values:
    packing: ""
    #metricsServer: true
